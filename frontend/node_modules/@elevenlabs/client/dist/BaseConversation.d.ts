import { Callbacks, Mode, Status } from "./types.js";
import type { MCPToolApprovalConfig } from "./types.js";
import type { BaseConnection, SessionConfig, FormatConfig } from "./utils/BaseConnection.js";
import { type UploadFileResult } from "./utils/uploadFile.js";
import type { Conversation } from "./index.js";
import type { AgentAudioEvent, AgentChatResponsePartEvent, AgentReasoningResponsePartEvent, AgentResponseEvent, AgentResponseCorrectionEvent, AgentTypingEvent, ClientToolCallEvent, ContextUsageEvent, ExternalAgentConnectedEvent, ExternalAgentDisconnectedEvent, InternalTentativeAgentResponseEvent, InterruptionEvent, PingEvent, RichContentEvent, UserTranscriptionEvent, VadScoreEvent, MCPToolCallClientEvent, AgentToolResponseEvent, AgentToolResponseFullPayloadEvent, ConversationMetadataEvent, AsrInitiationMetadataEvent, MCPConnectionStatusEvent, ErrorMessageEvent, AgentToolRequestEvent, GuardrailTriggeredEvent } from "./utils/events.js";
import type { InputConfig } from "./InputController.js";
import type { OutputConfig } from "./OutputController.js";
export type { Role, Mode, Status, Callbacks, MCPToolApprovalRequest, MCPToolApprovalRequestContext, MCPToolApprovalHandler, MCPToolApprovalConfig, } from "./types.js";
export { CALLBACK_KEYS } from "./types.js";
export type { UploadFileResult } from "./utils/uploadFile.js";
/** Allows self-hosting the worklets to avoid whitelisting blob: and data: in the CSP script-src  */
export type AudioWorkletConfig = {
    workletPaths?: {
        rawAudioProcessor?: string;
        audioConcatProcessor?: string;
    };
    libsampleratePath?: string;
};
export type ConversationCreatedCallback = (conversation: Conversation) => void;
export type ConversationLifecycleOptions = {
    onConversationCreated?: ConversationCreatedCallback;
};
export type ContextualUpdateOptions = {
    contextId?: string;
};
/**
 * Attribution for a user message that came from a rich-content button tap.
 * Best-effort: a server that ignores it loses nothing but the reporting, and
 * the message text alone is what the agent acts on.
 */
export type SendUserMessageOptions = {
    /** The rich-content row the tapped button belonged to. */
    richContentId?: string;
};
export type Options = SessionConfig & Callbacks & ConversationLifecycleOptions & ClientToolsConfig & MCPToolApprovalConfig & InputConfig & OutputConfig & AudioWorkletConfig;
export type PartialOptions = SessionConfig & Partial<Callbacks> & ConversationLifecycleOptions & Partial<ClientToolsConfig> & Partial<MCPToolApprovalConfig> & Partial<InputConfig> & Partial<OutputConfig> & Partial<FormatConfig> & Partial<AudioWorkletConfig>;
export type MultimodalMessageInput = {
    text?: string;
    /** @deprecated Use `fileIds`. */
    fileId?: string;
    fileIds?: string[];
};
export type ClientToolsConfig = {
    clientTools: Record<string, (parameters: any) => Promise<string | number | void> | string | number | void>;
};
export declare function isTextOnly(options: PartialOptions): boolean | undefined;
export declare abstract class BaseConversation {
    protected readonly options: Options;
    protected readonly connection: BaseConnection;
    protected lastInterruptTimestamp: number;
    protected mode: Mode;
    protected status: Status;
    protected volume: number;
    protected currentEventId: number;
    protected canSendFeedback: boolean;
    /**
     * Every MCP tool call this conversation has dispatched to
     * `onMCPToolApprovalRequest`, keyed by `tool_call_id`. Entries are kept for
     * the lifetime of the conversation on purpose: dropping a settled entry
     * would let a repeated `awaiting_approval` event run the handler a second
     * time and put a second, possibly contradictory, result on the wire.
     */
    private readonly mcpApprovals;
    protected static getFullOptions(partialOptions: PartialOptions): Options;
    protected constructor(options: Options, connection: BaseConnection);
    protected markConnected(): void;
    endSession(): Promise<void>;
    private endSessionWithDetails;
    protected handleEndSession(): Promise<void>;
    protected updateMode(mode: Mode): void;
    protected updateStatus(status: Status): void;
    protected updateCanSendFeedback(): void;
    protected handleInterruption(event: InterruptionEvent): void;
    protected handleAgentResponse(event: AgentResponseEvent): void;
    protected handleAgentResponseCorrection(event: AgentResponseCorrectionEvent): void;
    protected handleUserTranscript(event: UserTranscriptionEvent): void;
    protected handleTentativeAgentResponse(event: InternalTentativeAgentResponseEvent): void;
    protected handleVadScore(event: VadScoreEvent): void;
    protected handlePing(event: PingEvent): void;
    protected handleContextUsage(event: ContextUsageEvent): void;
    protected handleClientToolCall(event: ClientToolCallEvent): Promise<void>;
    protected handleAudio(event: AgentAudioEvent): void;
    protected handleMCPToolCall(event: MCPToolCallClientEvent): Promise<void>;
    /**
     * Sends the one approval result this `tool_call_id` is allowed, or drops a
     * decision that arrived too late to mean anything.
     */
    private settleMCPApproval;
    private markMCPApprovalStale;
    private mcpApprovalErrorContext;
    protected handleMCPConnectionStatus(event: MCPConnectionStatusEvent): void;
    protected handleAgentToolRequest(event: AgentToolRequestEvent): void;
    protected handleAgentToolResponse(event: AgentToolResponseEvent): void;
    protected handleAgentToolResponseFullPayload(event: AgentToolResponseFullPayloadEvent): void;
    protected handleConversationMetadata(event: ConversationMetadataEvent): void;
    protected handleAsrInitiationMetadata(event: AsrInitiationMetadataEvent): void;
    protected handleAgentChatResponsePart(event: AgentChatResponsePartEvent): void;
    protected handleAgentReasoningResponsePart(event: AgentReasoningResponsePartEvent): void;
    protected handleRichContent(event: RichContentEvent): void;
    protected handleGuardrailTriggered(_event: GuardrailTriggeredEvent): void;
    protected handleAgentTyping(event: AgentTypingEvent): void;
    protected handleExternalAgentConnected(_event: ExternalAgentConnectedEvent): void;
    protected handleExternalAgentDisconnected(_event: ExternalAgentDisconnectedEvent): void;
    protected handleErrorEvent(event: ErrorMessageEvent): void;
    private onMessage;
    private onError;
    private onOutgoingMessage;
    getId(): string;
    isOpen(): boolean;
    abstract setVolume(options: {
        volume: number;
    }): void;
    abstract setMicMuted(isMuted: boolean): void;
    /**
     * Returns byte frequency data (0-255) for the input audio, focused on the
     * human voice range (100-8000 Hz).
     */
    abstract getInputByteFrequencyData(): Uint8Array;
    /**
     * Returns byte frequency data (0-255) for the output audio, focused on the
     * human voice range (100-8000 Hz).
     */
    abstract getOutputByteFrequencyData(): Uint8Array;
    abstract getInputVolume(): number;
    abstract getOutputVolume(): number;
    sendFeedback(like: boolean | null, eventId?: number): void;
    sendContextualUpdate(text: string, options?: ContextualUpdateOptions): void;
    sendUserMessage(text: string, options?: SendUserMessageOptions): void;
    sendUserActivity(): void;
    sendMCPToolApprovalResult(toolCallId: string, isApproved: boolean): void;
    sendMultimodalMessage(options: MultimodalMessageInput): void;
    uploadFile(file: Blob): Promise<UploadFileResult>;
}
//# sourceMappingURL=BaseConversation.d.ts.map